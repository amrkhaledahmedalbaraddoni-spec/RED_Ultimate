# Copy to .env and override before running docker compose.
# Generate strong secrets, e.g.:  openssl rand -hex 32

RED_JWT_SECRET=please-change-this-long-random-sovereign-secret-32b
RED_ADMIN_EMAIL=admin@red.local
RED_ADMIN_PASSWORD=changeme123

# Reachable IP for WebRTC ICE candidates (your server's LAN/public IP)
PUBLIC_IP=192.168.1.50

TURN_SECRET=redturnsecret
