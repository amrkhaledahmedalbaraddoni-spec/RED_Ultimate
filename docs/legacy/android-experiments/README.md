# Archived Android prototype

This folder is **not** an Android application and is not included by any Gradle settings file.
It preserves the original experimental RED sketches for reference so no source material is lost.
The buildable RED features are in the single production module:

```text
app/src/main/java/com/red
```

Do not open or build this folder. It has no active `settings.gradle` or `build.gradle` file.
